# ARK Core-Only Configuration
# Minimal resource footprint - Essential infrastructure only
#
# Usage:
#   cp examples/.env.core-only .env
#   source .env
#   ./scripts/ark-manager.sh deploy

# Docker Compose Profiles
# Only deploy core infrastructure services
COMPOSE_PROFILES=core

# Optional: Override default paths if needed
# ARK_DIR=/opt/ark
# LOG_DIR=/opt/ark/logs

# Optional: Tailscale Auth Key (if not set in vessel_secrets.env)
# TAILSCALE_AUTHKEY=

# Resource Requirements:
# - RAM: ~2GB
# - Storage: ~5GB
# - CPU: 2 cores
#
# Services Included:
# - Traefik (reverse proxy)
# - Tailscale (mesh networking)
# - Autoheal (container watchdog)
# - Homepage (dashboard)
# - Portainer (container management)
# - Syncthing (file sync)
